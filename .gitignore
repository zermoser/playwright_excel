# ─── Node / Yarn ───────────────────────────────────────────────────────────────
node_modules/
dist/
**/dist/
build/
**/build/
yarn-error.log
yarn-debug.log*
.pnp.*
.cache/

# ─── Vite ─────────────────────────────────────────────────────────────────────
# (ผลลัพธ์จากการ build ของ Vite)
.vite/

# ─── Environment ──────────────────────────────────────────────────────────────
.env
.env.*.local
.env.local
.env.development.local
.env.test.local
.env.production.local

# ─── TypeScript ────────────────────────────────────────────────────────────────
# ถ้าใช้ tsconfig.json แบบมีสร้างไฟล์ .tsbuildinfo
*.tsbuildinfo

# ─── Editor / OS ───────────────────────────────────────────────────────────────
# macOS
.DS_Store

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# VSCode
.vscode/
.history/

# JetBrains IDEs (WebStorm, IntelliJ)
.idea/

# ─── Logs ───────────────────────────────────────────────────────────────────────
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# ─── Misc ───────────────────────────────────────────────────────────────────────
*.log
*.lock
.sass-cache/
.cache/
*.tmp
*.temp

# ─── Tailwind / PostCSS ─────────────────────────────────────────────────────────
# (มักไม่มีไฟล์ออกมาใน root ยกเว้นคุณตั้งค่าให้ generate)
public/**/*.css
public/**/*.map

# ─── GitHub Pages (ถ้าใช้ฟีเจอร์ gh-pages) ────────────────────────────────────
.gh-pages/

# ─── Coverage (ถ้าใช้ Jest หรือ Testing อื่น ๆ) ─────────────────────────────────
coverage/
*.lcov

# ─── OS Generated Files ─────────────────────────────────────────────────────────
*.swp
*~

# ─── Playwright Test Artifacts ──────────────────────────────────────────────────
# ละเว้นโฟลเดอร์รายงานผลทดสอบ (report) ของ Playwright
playwright-report/
# ละเว้นโฟลเดอร์ผลลัพธ์ test-result (หากใช้ output ชื่ออื่น เช่น test-results)
test-results/
# ละเว้นไฟล์ trace, artifacts, snapshots
playwright-test-results/
# ละเว้นโฟลเดอร์ screenshot/video/trace ที่ Playwright บันทึก (หากตั้งใน config)
**/test-results/**         # อาจใช้ pattern ทั่วไปหากวางในหลายที่
**/playwright-report/**    # หากวาง report ไว้ภายใน project หลายที่
**/trace/**                # ถ้าตั้งบันทึก trace ลงโฟลเดอร์นี้
**/screenshots/**          # ถ้าบันทึกภาพหน้าจอขณะทดสอบ
**/videos/**               # ถ้าบันทึกวิดีโอตอนทดสอบ
# (ถ้าใช้ชื่ออื่นในการ config Playwright ให้ปรับชื่อ folder/pattern ตามนั้น)

# ─── เพิ่มเติม (ถ้ามี) ────────────────────────────────────────────────────────
# ละเว้นไฟล์ผลลัพธ์จากการรันทดสอบอื่นๆ หรือ artifacts ต่างๆ ตามต้องการ:
# เช่น Cypress, Jest snapshots ฯลฯ
